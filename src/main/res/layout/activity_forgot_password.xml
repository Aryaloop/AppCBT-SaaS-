<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:padding="24dp">

    <ImageView
        android:id="@+id/ivIcon"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@android:drawable/ic_dialog_email"
        app:tint="@color/signature_blue"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="64dp"/>

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pulihkan Akun"
        android:textColor="@color/slate_900"
        android:textSize="32sp"
        android:fontFamily="@font/calistoga"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/ivIcon" />

    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Masukkan email yang terdaftar. Kami akan mengirimkan tautan untuk mengatur ulang kata sandi Anda."
        android:textColor="@color/slate_600"
        android:textSize="16sp"
        android:lineSpacingExtra="4dp"
        android:fontFamily="@font/inter_regular"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle" />

    <TextView
        android:id="@+id/tvEmailLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Alamat Email"
        android:textColor="@color/slate_900"
        android:textSize="14sp"
        android:fontFamily="@font/inter_bold"
        android:layout_marginTop="40dp"
        android:layout_marginStart="4dp"
        app:layout_constraintTop_toBottomOf="@id/tvSubtitle"
        app:layout_constraintStart_toStartOf="parent" />

    <EditText
        android:id="@+id/etForgotEmail"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:hint="Alamat Email Anda"
        android:background="@drawable/bg_input_field"
        android:paddingHorizontal="20dp"
        android:inputType="textEmailAddress"
        android:fontFamily="@font/inter_regular"
        android:layout_marginTop="8dp"

        android:textColor="@color/slate_900" android:textColorHint="@color/slate_600"

        app:layout_constraintTop_toBottomOf="@id/tvEmailLabel"
        app:layout_constraintStart_toStartOf="parent"/>

    <Button
        android:id="@+id/btnSendResetLink"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Kirim Tautan Pemulihan"
        android:background="@drawable/btn_primary_gradient"
        app:backgroundTint="@null"
        android:textAllCaps="false"
        android:textSize="16sp"
        android:fontFamily="@font/inter_bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/etForgotEmail" />

</androidx.constraintlayout.widget.ConstraintLayout>